# PixelProof 隐私说明

PixelProof 用于对比 Figma 选区与用户指定的开发页面。插件本身不将截图、DOM/CSS、Token 数据或源码上传到 PixelProof 服务器。

- Figma 选区在插件内导出为 PNG，仅在当前插件会话中处理。
- 开发页由用户电脑上的 PixelProof Companion 通过系统 Edge/Chrome 打开。
- 伴侣只监听 `localhost:49321`，且每次启动使用新的会话令牌。
- URL 历史默认不保存；报告默认移除查询参数、用户名、密码和 hash。
- 源码扫描默认关闭；只有用户明确填写本地目录时才执行只读检查。
- 报告仅在用户点击导出时写入本地下载位置。

如目标页面会访问第三方网络资源，该请求由目标页面自身发起，受其隐私政策约束。
